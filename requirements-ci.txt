# CI-only subset of requirements.txt -- deliberately excludes
# apache-airflow. Airflow's own setup (metadata DB migration, its own
# heavy dependency tree) is real infra work, but it doesn't add
# correctness coverage here: the DAG's tasks are just subprocess calls to
# `dbt run`/`dbt test`/`write_gold_to_delta.py`, which tests/test_pipeline.py
# already exercises directly. Paying Airflow's install/setup cost on
# every CI run for that would be pure overhead. Airflow orchestration
# itself is verified locally (see medallion-pipeline's README) -- CI
# focuses on the actual data/business logic.
dbt-core>=1.10
dbt-duckdb>=1.10
deltalake>=1.6
pyarrow>=17.0
pytest>=8.0
